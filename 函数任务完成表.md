# 实验一：函数实现映射文档

本文档列出了实验任务中要求的各个函数在 `tiny-compiler-rs` 项目中的具体实现位置。由于 Rust 的方法签名和重载机制与 C++ 不同，部分函数根据不同的参数类型分别对应了不同的 Rust 函数实现。

## 1. 字符集的创建与运算

这部分代码全部实现在 `src/lab1/char_set.rs` 文件中。

| 实验任务要求签名 | Rust 实现函数名 | 源码链接 |
| :--- | :--- | :--- |
| `int range(char fromChar, char toChar)` | `range(from_char, to_char)` | [char_set.rs:88](./src/lab1/char_set.rs#L88) |
| `int union(char c1, char c2)` | `union_chars(c1, c2)` | [char_set.rs:111](./src/lab1/char_set.rs#L111) |
| `int union(int charSetId, char c)` | `union_charset_char(char_set_id, c)` | [char_set.rs:168](./src/lab1/char_set.rs#L168) |
| `int union(int charSetId1, int charSetId2)` | `union_charsets(char_set_id1, char_set_id2)` | [char_set.rs:275](./src/lab1/char_set.rs#L275) |
| `int difference(int charSetId, char c)` | `difference_charset_char(char_set_id, c)` | [char_set.rs:382](./src/lab1/char_set.rs#L382) |

## 2. NFA 构造法正规运算

这部分代码实现在 `src/lab1/graph.rs` 文件中，用于 NFA 图结构的构建和正规运算。

| 实验任务要求签名 | Rust 实现函数名 | 源码链接 |
| :--- | :--- | :--- |
| `Graph * generateBasicNFA(...)` | `generateBasicNFA(...)` | [graph.rs:1720](./src/lab1/graph.rs#L1720) |
| `Graph * union(Graph *pNFA1, Graph *pNFA2)` | `union(g1, g2)` | [graph.rs:1788](./src/lab1/graph.rs#L1788) |
| `Graph * product(Graph *pNFA1, Graph *pNFA2)` | `product(g1, g2)` | [graph.rs:1934](./src/lab1/graph.rs#L1934) |
| `Graph * plusClosure(Graph *pNFA)` | `plusClosure(g)` | [graph.rs:2012](./src/lab1/graph.rs#L2012) |
| `Graph * closure(Graph *pNFA)` | `closure(g)` | [graph.rs:2228](./src/lab1/graph.rs#L2228) |
| `Graph * zeroOrOne(Graph *pNFA)` | `zeroOrOne(g)` | [graph.rs:2410](./src/lab1/graph.rs#L2410) |

## 3. NFA 转化为 DFA

这部分代码实现在 `src/lab1/graph.rs` 文件中，并作为 `Graph` 结构体的方法 (`impl Graph`) 给出。

| 实验任务要求签名 | Rust 实现函数名 | 源码链接 |
| :--- | :--- | :--- |
| `move` (子集构造法) | `move_by_driver` / `move_by_char` | [graph.rs:1023](./src/lab1/graph.rs#L1023) / [graph.rs:1059](./src/lab1/graph.rs#L1059) |
| `ε_closure` (子集构造法) | `epsilon_closure(curr)` | [graph.rs:1097](./src/lab1/graph.rs#L1097) |
| `DTran` (子集构造法) | `DTran_driver` / `DTran_char` | [graph.rs:1145](./src/lab1/graph.rs#L1145) / [graph.rs:1173](./src/lab1/graph.rs#L1173) |
| `Graph * NFA_to_DFA(Graph *pNFA)` | `NFA_to_DFA(&self)` | [graph.rs:1197](./src/lab1/graph.rs#L1197) |

## 4. 最小化 NFA (包含构造期与后处理优化)

这一部分涉及 Thompson 构造法期间的结构优化（入度出度判断），以及针对完整的 NFA 图网络进行节点合并、边合并裁剪的后段化简过程。实现在 `src/lab1/graph.rs` 文件中。

| 实验任务要求签名 / 优化功能 | Rust 实现函数名 | 源码链接 |
| :--- | :--- | :--- |
| NFA 结构闭包化简 (判断入边) | `has_incoming_edge(edges, state_id)` | [graph.rs:2086](./src/lab1/graph.rs#L2086) |
| NFA 结构闭包化简 (判断出边) | `has_outgoing_edge(edges, state_id)` | [graph.rs:2112](./src/lab1/graph.rs#L2112) |
| 闭包化简保守构造法 | `closure_conservative(mut g)` | [graph.rs:2132](./src/lab1/graph.rs#L2132) |
| 核心化简调度 (后处理) | `minimize_nfa(&self)` | [graph.rs:305](./src/lab1/graph.rs#L305) |
| 消除 $\varepsilon$-边 | `build_epsilon_free_nfa(&self)` | [graph.rs:327](./src/lab1/graph.rs#L327) |
| 修剪无用状态 | `prune_useless_states(&self)` | [graph.rs:645](./src/lab1/graph.rs#L645) |
| 合并等价状态 | `merge_equivalent_states(&self)` | [graph.rs:773](./src/lab1/graph.rs#L773) |

